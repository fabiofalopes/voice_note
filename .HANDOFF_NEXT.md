# 🚨 NEXT SESSION HANDOFF

> **⚠️ STALE / SUPERSEDED (2026-07-19).** This handoff is 15 months old (created 2025-04-02). Its mandate ("make robust the default") is now **Stream D work, optional for v1.0 ship** — see [`AGENTS.md`](./AGENTS.md) §3. Pre-Stream-A foundation (commit + tests) and Stream A (contract) come FIRST. Stream D closes this handoff when executed; until then, this file is retained as the canonical motivating story (4.5-hour data loss).

## Priority 1: Make Robust Recording the DEFAULT

> **Note (2026-07-19):** Priority rebalanced. Pre-Stream-A + Stream A are the current Priority 1. Stream D runs in parallel, optionally.

**Status**: Robust recorder implemented, needs to be made default

### What Happened
User lost 4.5 hours of recording when earbuds disconnected. Standard recorder keeps ALL audio in RAM and crashes on device failure.

### The Fix Already Built
✅ Robust recorder with chunking implemented
✅ Device error handling implemented
✅ Auto-merge functionality implemented
❌ NOT the default yet (still opt-in with `--robust`)

### What Needs Doing

```bash
# Current state (UNSAFE)
vn                    # ← Loses everything on device failure

# Required state (SAFE)
vn                    # ← Should use robust by default
vn --legacy           # ← Only if explicitly requested (with warning)
```

### Quick Start for Next Session

1. Read `RELIABILITY_FIRST.md` (5 min) - Complete philosophy
2. Edit `src/cli.py` - Change default: `robust=True`
3. Add `--legacy` flag with warning
4. Test with various scenarios
5. Update README

### Success Criteria
- [ ] `vn` uses robust by default
- [ ] No recording can lose more than 5 minutes
- [ ] Device failures are handled gracefully
- [ ] Documentation updated

### Files Created This Session
- `src/audio_processing/robust_recorder.py` - Fault-tolerant recorder
- `RELIABILITY_FIRST.md` - Project manifesto
- `RELIABILITY_FIX.md` - Technical analysis
- `ROBUST_RECORDING.md` - User guide

### Core Principle
**"We will not rest until users can trust voice_note with their most important recordings."**

---

*Created: 2025-04-02*
*Priority: CRITICAL*
